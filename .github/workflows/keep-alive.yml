{name: Keep Render Awake, on: {schedule: [{cron: '*/10 0-12 * * 1-5'}], workflow_dispatch: {}}, jobs: {ping: {runs-on: ubuntu-latest, steps: [{name: 'Ping Render to prevent sleep', run: 'curl -sS -o /dev/null -w "HTTP %{http_code}\n" --max-time 20 https://etf-poject.onrender.com/ || true'}]}}}
